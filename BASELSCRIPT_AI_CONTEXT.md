# BaselScript AI Context

This is the canonical entry point for AI systems working with BaselScript.

## Mandatory answer gate

Before generating, reviewing, explaining, validating, or modifying BaselScript code:

1. read `manifest.json`
2. load every file listed in `loading_policy.baseline_required`
3. classify the user's request into one or more `task_routes`
4. load every `required_files` entry for every matching route
5. only then answer with BaselScript syntax

If a route has `hard_gate=true`, this sequence is mandatory.

If a required routed file cannot be accessed, state which file could not be loaded and do not invent
that domain's syntax.

If several routes apply, load all of them. For example, a graphic scene that reads a file requires
both the `graphics` and `files_directories` routes.

## Source authority

```text
confirmed current runtime/regression
> current language/*.def
> generated language/baselscript-language.json
> current knowledge/*.md
> repeated real script corpus
> rare corpus evidence
> historical/review examples
```

The `.def` files are the formal contract.
`baselscript-language.json` is generated by the interpreter and must not be hand-edited.

## Current contract snapshot

```text
loaded      true
loadErrors  empty
functions   247
actions     126
blocks      7
scenes      1
conditions  16
```

Current interpreter source directory:

```text
c:/baselscript/system/language/
```

## Hard generation rule

Do not invent:

- actions
- functions
- parameters
- blocks
- condition operators
- closing keywords
- CALL target families
- graphics tiles
- CHART syntax
- platform APIs

If a construct cannot be confirmed, label it unverified instead of guessing.

Do not ask the user for syntax information that is already present in a routed reference file.
When the routed file contains an exact working pattern, use that pattern.

## Deterministic task routing

Use `manifest.json -> task_routes` as the machine-readable routing table.

Minimum mandatory routing:

```text
Graphics / canvas / draw   -> knowledge/graphics_status.md
Charts / CHART_*           -> knowledge/charts_status.md
UI / MENU / LIST / FORM    -> knowledge/ui_status.md
Files / directories        -> knowledge/files_status.md
Database / SQL             -> knowledge/database_status.md
Crypto / security          -> knowledge/security_status.md
Functions                  -> knowledge/functions_status.md + language/functions.def
Date / time / weekday      -> knowledge/date_time_status.md + knowledge/functions_status.md + language/functions.def
CALL / scene / section     -> knowledge/call_execution_status.md
IF / loops / testing       -> knowledge/testing_status.md + knowledge/regression_status.md
Network / device / media   -> knowledge/network_device_status.md
Script/meta operations     -> knowledge/script_meta_status.md
Weak-contract actions      -> knowledge/weak_actions_status.md
```

### Graphics hard gate

For any request involving `graphic`, `canvas`, `draw`, `circle`, `ellipse`, `rect`, `line`, `point`,
`text`, `image`, `polygon`, `property`, `prop`, `sector`, `arc`, or `segment`:

1. read `knowledge/graphics_status.md`
2. read `language/actions.def`
3. do not answer with graphics syntax before both are loaded

If `graphics_status.md` contains an exact working pattern, use it directly rather than saying that
the syntax is unknown.

Example: if the user gives canvas dimensions and asks for a centered circle, compute the center from
the dimensions and use the documented circle syntax. Do not request the dimensions again and do not
claim the circle syntax is unavailable if it is present in `graphics_status.md`.

### Charts hard gate

For any `CHART_*` request, read `knowledge/charts_status.md` before generating code.

### UI hard gate

For MENU, LIST, DIALOG, FORM, POPUPMENU, tiles, or dynamic UI, read `knowledge/ui_status.md` before
generating code.

### Database/security hard gate

For database/SQL or security/crypto questions, read the corresponding status file first. Keep
ApplicationDatabase and SystemDatabase semantics separate.

## Explicitly removed standalone actions

Do not generate:

```text
db_current
db_path
db_exists
open
grid
pdf
mail
vibrate
```

Use standalone `select` for the current file-picker workflow.

Android notification vibration is a notification feature and is not evidence for a standalone
`vibrate` action.

## Known validator issue

Dynamic MENU is valid CURRENT runtime syntax:

```baselscript
clear menu=m2
create tile=title text="menu 2"
create tile=item1 text="option" sec=e11
call menu=m2
```

The currently installed validator build can still report that the dynamic MENU does not exist.
Treat this as an open Validator/build synchronization defect, not as invalid language syntax.



## MANDATORY ROUTING RULE

If `manifest.json` contains one or more matching `task_routes` with:

```text
hard_gate = true
```

you MUST load every file listed in `required_files` for every matching hard-gate route before answering.

Do not answer from baseline files alone when a matching hard-gate route exists.

A task is not considered:

```text
BaselScript reference loaded
```

until all of the following have been successfully read:

1. every file in `loading_policy.baseline_required`
2. every `required_files` entry for all matching `task_routes` where `hard_gate=true`

If any required routed file cannot be loaded:

1. explicitly name the file that could not be loaded
2. do not generate BaselScript code for that task
3. do not substitute general programming knowledge
4. do not guess unsupported syntax

For example, a weekday/date request matches the `date_time` route. Before answering, the AI must read:

```text
knowledge/date_time_status.md
knowledge/functions_status.md
language/functions.def
```

Only after those files are loaded may the AI generate date/time BaselScript.


## Semantic completeness rule

The machine-readable `.def` and JSON files do not describe every runtime side effect.

Before saying that a requested operation is unsupported:

1. load the matching `task_routes` knowledge file
2. check system/output variables produced by the function
3. check documented compositions of CURRENT primitives
4. prefer current runtime/reference evidence over absence of a convenience function name

Concrete example:

```baselscript
#d=$date()
message #_current_weekday_name
```

This is valid current-date weekday-name behavior even though there is no dedicated
`weekday_name(...)` function.

For an arbitrary date, read `knowledge/date_time_status.md` before answering.



When you say "BaselScript reference loaded", that statement is only valid after all baseline files
and all hard-gate route files required for the current task have been successfully read.


## Recommended ChatGPT bootstrap prompt

Use the current official BaselScript reference before generating, reviewing, explaining, validating,
or modifying BaselScript code.

First fetch and read:

```text
https://raw.githubusercontent.com/BaselFil/baselscript-training/main/manifest.json
```

Then follow `loading_policy` and `task_routes` exactly:

1. load all `baseline_required` files
2. identify every matching task route
3. load all `required_files` for those routes
4. only then produce BaselScript syntax

Treat every `hard_gate=true` route as mandatory.

Do not invent unsupported BaselScript commands, parameters, functions, blocks, graphics tiles, CHART
operations, or platform behavior. If a required routed file cannot be accessed, say which file is
unavailable before generating domain-specific BaselScript code.
