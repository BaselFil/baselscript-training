# Unverified / not yet promoted

Do not generate these merely because they may appear in historical scripts or prior notes.

Current rule:

- if a spelling is absent from the current machine contract and is not explicitly runtime-confirmed in a semantic file, treat it as unverified
- if a construct is present only as a broad Prefix family, do not invent arbitrary members of that prefix family

Items requiring future audit include broad legacy prefixes such as:

```text
fil*
timer*
notif*
thumb*
directory_l*
dir_l*
search_dir*
```

Their presence proves a runtime-compatible prefix family in the validator contract, not every possible concrete spelling.
