# Weak-contract safe test candidates

These tests were derived from current `actions.def`, the Windows checkpoint runtime, and existing
confirmed regression patterns. They are intentionally marked TEST CANDIDATE until executed by the
maintainer on the current interpreter build.

Do not copy a PASS result into `regression_status.md` until the test was actually executed.
