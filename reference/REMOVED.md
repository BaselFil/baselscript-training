# Removed / do not generate

These names are intentionally absent from the current machine contract and must not be generated as current BaselScript actions unless a future runtime version reintroduces and documents them:

```text
open
grid
pdf
mail
vibrate
db_current
db_path
db_exists
```
