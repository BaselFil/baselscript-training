# Semantic rebuild completion status

All 18 semantic category files now exist.

Important rule:
`complete file set` does not mean `all semantics fully verified`.

Categories explicitly marked PARTIAL remain constrained. AI must not fill missing grammar by analogy.

High-confidence categories:
- core invocation rules
- structure
- CALL families
- IF/operators
- current UI core patterns
- function catalog/arity
- date/current-weekday semantics
- SQL family separation
- crypto/security separation
- graphics primitives
- charts lifecycle
- testing actions

Still requiring future runtime audit:
- complete file/data action parameters
- complete array/hash declaration/mutation grammar
- every UI tile/parameter combination
- broad Prefix action concrete members
- network/media/device detailed parameters
- script/meta destructive/side-effect behavior
- Android-specific behavior
